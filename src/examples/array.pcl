program test;
var a : array [2] of array [2] of integer;
var i,j : integer;
begin
  i := 0;
  j := 0;
  while i < 2 do
  begin
    while j < 2 do
    begin
      a[i][j] := 10*i + j;
      writeInteger(a[i][j]);
      j := j + 1;
   end;
   j := 0;
   i := i + 1;
  end;
end.
