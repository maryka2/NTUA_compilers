program test;
var i : integer;
var j : integer;
begin
  i := 0;
  while i < 5 do
  begin
    j := i;
    while j < 5 do
    begin
      writeInteger(j);
      j := j + 1
    end;
    i := i + 1
  end
end.
